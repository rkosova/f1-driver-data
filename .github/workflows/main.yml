on:
  push:
    branches:
    - master
    - release/*
  workflow_dispatch:

jobs:
  my_job:
    name: My Job
    runs-on: self-hosted
    steps:
      - run: docker run hello-world
        name: Basic output
