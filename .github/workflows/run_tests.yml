name: Run Tests

on:
  push:
    branches:
      - '*'

jobs:
  build:
    runs-on: kjel
  
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      
      - name: Run tests
        run: docker build -t tests_container . 
