name: Run Tests

on:
  push:
    branches:
      - '*'

jobs:
  build:
    runs-on: kjel

    container:
      image: Dockerfile

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Run tests
        run: pytest
