name: Create pandocs pdf

on:
  push:
    branches:
      - '*'

jobs:
  make_pandoc:
    runs-on: kjellrunner

    steps: 
      - uses: actions/checkout@v4

      - name: something
        run: ls
      
