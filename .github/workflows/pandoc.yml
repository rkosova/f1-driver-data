name: Create pandocs pdf

on:
  push:
    branches:
      - '*'

jobs:
  make_pandoc:
    runs-on: self-hosted
    steps: 
      - uses: actions/checkout@v4

      - name: something
        run: ls
      
